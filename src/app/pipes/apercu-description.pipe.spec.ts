import { ApercuDescriptionPipe } from './apercu-description.pipe';

describe('ApercuDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new ApercuDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
